#include <stdio.h>
#include <stdlib.h>
#include <cs50.h>

int main(void)
{
    int *x = malloc(3 * sizeof(int));

    // char *s = get_string("s: ");
    x[0] = 72;
    x[1] = 73;
    x[2] = 33;

    printf("");

    return 0;
}