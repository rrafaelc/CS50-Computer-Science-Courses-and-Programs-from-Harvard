#include <cs50.h>
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

int main(int argc, string argv[])
{
    if (argc == 1 || argc > 2)
    {
        // Check if has no command
        printf("Usage: ./caesar key\n");

        return 1;
    }

    // printf("-> %i\n", (('Y' - 65) + 3) % 26);
}